language: cpp
dist: bionic
compiler: g++
sudo: required

before_install:
  - add-apt-repository ppa:beineri/opt-qt-5.12.0-bionic
  - apt-get update

install:
  - apt-get install qt512base 

before_script:
  - QTDIR="/opt/qt512"
  - PATH="$QTDIR/bin:$PATH"
  - source /opt/qt512/bin/qt512-env.sh
  - qmake Tibi__SharingOnLAN.pro

script:
  - make
